.next
node_modules
.env*
!.env.example
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
coverage
.vercel

# clerk configuration (can include secrets)
/.clerk/

# local Cursor IDE config
.cursor/

# Convex `_generated` is committed so `next build` on Vercel/GitHub works without a local `convex dev`.
# Regenerate after function/schema changes: `npx convex codegen` (or `npx convex dev`).

